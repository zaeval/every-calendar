<template>
    <div class="loader-background" ref="loaderBackground">
        <div class="loader">
            <svg viewBox="0 0 80 80">
                <circle id="test" cx="40" cy="40" r="32"></circle>
            </svg>
        </div>
        <div class="loader">
            <svg viewBox="0 0 80 80">
                <rect x="8" y="8" width="64" height="64"></rect>
            </svg>
        </div>
    </div>
</template>

<script>
    export default {
        name: "Loading",
        methods: {
            show: function () {
                var el = this.$refs.loaderBackground;
                Velocity(el, {
                    opacity: 1
                }, {
                    duration: 200,
                    display:'flex',
                });
            },
            hide: function () {
                var el = this.$refs.loaderBackground;
                Velocity(el, {
                    opacity: 0
                }, {
                    duration: 200,
                    display:'none',
                });
            }
        }
    }
</script>

<style scoped>
    .loader {
        --path: #ffd763;
        --duration: 3s;
        width: 44px;
        height: 44px;
        position: relative;
    }

    .loader:before {
        content: '';
        width: 6px;
        height: 6px;
        border-radius: 50%;
        position: absolute;
        display: block;
        background: var(--dot);
        top: 37px;
        left: 19px;
        transform: translate(-18px, -18px);
        animation: dotRect var(--duration) cubic-bezier(0.785, 0.135, 0.15, 0.86) infinite;
    }

    .loader svg {
        display: block;
        width: 100%;
        height: 100%;
    }

    .loader svg rect,
    .loader svg circle {
        fill: none;
        stroke-width: 10px;
        stroke-linejoin: round;
        stroke-linecap: round;
    }

    .loader svg rect {
        stroke: #70c3ff;
        stroke-dasharray: 192 64 192 64;
        stroke-dashoffset: 0;
        animation: pathRect 3s cubic-bezier(0.785, 0.135, 0.15, 0.86) infinite;
    }

    .loader svg circle {
        stroke: #ffd763;
        stroke-dasharray: 150 50 150 50;
        stroke-dashoffset: 75;
        animation: pathCircle var(--duration) cubic-bezier(0.785, 0.135, 0.15, 0.86) infinite;
    }

    @keyframes pathRect {
        25% {
            stroke-dashoffset: 64;
        }

        50% {
            stroke-dashoffset: 128;
        }

        75% {
            stroke-dashoffset: 192;
        }

        100% {
            stroke-dashoffset: 256;
        }
    }

    @keyframes dotRect {
        25% {
            transform: translate(0, 0);
        }

        50% {
            transform: translate(18px, -18px);
        }

        75% {
            transform: translate(0, -36px);
        }

        100% {
            transform: translate(-18px, -18px);
        }
    }

    @keyframes pathCircle {
        25% {
            stroke-dashoffset: 125;
        }

        50% {
            stroke-dashoffset: 175;
        }

        75% {
            stroke-dashoffset: 225;
        }

        100% {
            stroke-dashoffset: 275;
        }
    }

    .loader {
        display: inline-block;
        margin: 0 8px;
    }

    .loader-background {
        position: fixed;
        top: 0;
        left: 0;
        min-height: 100vh !important;
        min-width: 100vw !important;
        background: rgb(241,243,246,.9);
        display: none;
        justify-content: center;
        align-items: center;
        z-index:100000;
    }
</style>
